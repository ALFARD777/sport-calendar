# Public base URL of the API used by Next.js rewrites during build/runtime.
# Example: https://api.example.com
API_BASE_URL=http://localhost:5131

# Public API URL used by the browser directly (recommended for production).
# Example: https://your-api.up.railway.app
NEXT_PUBLIC_API_BASE_URL=
